export enum LabelTypeno {
    labelTypeno=1,
}

export enum LabelPageTypeno {
    None=0,
    QuestionPage=1,
    FormPage=2,
}
export enum Duplicate {
    None=0,
    Allow =1,
    TemplateAllow=2
}
